# 报告:审计 16 轮后的前瞻收敛(2026-08-17)

## 1. 摘要

针对"每次修改后不再引入新问题"的要求,对第 16 轮全部修改做
**前瞻性状态机自审**,修复 1 个自我引入的边界问题,并**增强自检**
把混合路径回归扼杀在本轮,而非留给下一轮审计。

## 2. 前瞻自审结果

### 已修复

| # | 问题 | 修复 |
|---|---|---|
| P1 | **on_tick 可能选中刚入队的 cur 自己**(其帧有效)→ 白做一次捕获/恢复循环(低效;状态一致但浪费) | `pick_next` 返回 cur 时撤销入队、恢复原帧;ctx_valid 保持 false(帧恢复不更新 ctx) |
| P2 | 混合路径无自检覆盖(抢占-协作交错的 ctx_valid/frame_valid 协议) | **sync 自检新增:8 线程 × 500 次互斥递增 + 偶次 yield**,校验总和 4000 —— 任何协议回归都会在此暴露 |

### 状态机完备性核对(通过)

- spawn/init/thread_entry/yield/block/wake/on_tick/exit 各路径的
  frame_valid/ctx_valid/woken 变迁逐一核对,无泄漏/陈旧残留;
- 帧恢复后 ctx_valid 语义正确(陈旧,待下次 yield 重存);
- ISR 零分配由容量预留保障(ready/reaper/threads reserve 64)。

## 3. 验证

- 门禁:dev+release / clippy / fmt / make test / make test-smp 全绿。
- 混合路径自检通过;基准:slab 18ns、上下文切换 281ns
  (含轮转扫描与混合测试的真实成本,可接受)。
- 20s 运行稳定无 panic。

## 4. 防回归承诺(后续每轮执行)

1. 每次修改后执行**状态机变迁核对**(受影响标志逐路径检查);
2. 新增测试覆盖本次修改触及的路径组合;
3. 文档/注册表同步于同一提交。
